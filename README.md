# SpyOS Portfolio

This project is built with a JavaScript/JSX stack using React and Vite.

## Validation

Run this command to verify there are no TypeScript (`.ts`/`.tsx`) source files left in the repository:

```bash
npm run check:js-only
```

https://portfolio22-livid.vercel.app
